export enum Points {
	ApproximateGuilds = 'approximate_guilds',
	Commands = 'commands',
	PerCoreCPULoad = 'per_core_cpu_load',
	Memory = 'memory',
	InteractionCount = 'interaction_count'
}

export enum Tags {
	Shard = 'shard',
	Client = 'client_id',
	Action = 'action',
	OriginEvent = 'origin_event'
}

export enum Actions {
	Addition = 'addition',
	Subtraction = 'subtraction',
	Sync = 'sync'
}
